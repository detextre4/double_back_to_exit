import 'package:flutter/material.dart';

class DoubleBackToExitWidget extends StatelessWidget {
  const DoubleBackToExitWidget({super.key, arMessage, required this.child});
  final Widget child;

  @override
  Widget build(BuildContext context) => child;
}
